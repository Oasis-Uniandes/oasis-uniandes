import React from 'react';

const Hero: React.FC = () => {
    return (
        <div className="hero">
            <h1>Welcome to the Hero component!</h1>
        </div>
    );
};

export default Hero;